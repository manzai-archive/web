---
import Base from '../layouts/Base.astro';
---
<Base title="关于 · 喜剧档案">
  <h1 class="text-3xl mb-6">关于</h1>

  <p class="mb-4 text-[15px] leading-relaxed">
    一个口语喜剧剧本档案，收录漫才、相声、单口、コント、落語等作品。本站只保存文字与元数据，不托管音视频，每条目链回原视频，希望读者去原片支持创作者。
  </p>

  <p class="mb-4 text-[15px] leading-relaxed">
    剧本由机器初步处理（抓取 → 转写 → 分说话人 → 错字纠正），再由人手工校对。欢迎 fork 仓库本地编辑或在 GitHub 上直接改 yaml，提 PR 即可。
  </p>

  <p class="mb-8 text-[15px] leading-relaxed">
    本站非营利、无广告、无追踪。版权方如需下架，请通过
    <a class="underline" href="https://github.com/manzai-archive/web/issues/new/choose" target="_blank" rel="noopener">GitHub Issue</a>
    联系。
  </p>

  <details class="mb-8 border rounded p-4 text-[14px]" style="border-color: var(--color-rule)">
    <summary class="cursor-pointer text-[var(--color-ink-soft)] font-medium">技术细节 / 协作流程</summary>

    <div class="mt-4 space-y-4 leading-relaxed" style="color: var(--color-ink-soft)">
      <div>
        <div class="font-medium mb-1 text-[var(--color-ink)]">数据结构</div>
        <p>每条作品对应两个文件，共享同一 slug：</p>
        <ul class="list-disc pl-5 mt-1 space-y-1">
          <li><code>src/content/manzai/&lt;slug&gt;.md</code>　元数据（标题、演员、来源、标签等）</li>
          <li><code>src/content/dialogues/&lt;slug&gt;.yaml</code>　结构化对话（每条 utterance 有 id / 时间戳 / 说话人 / 文字 / 翻译）</li>
        </ul>
        <p class="mt-1">数据与代码分离，便于 fork 后只改对话内容，git diff 干净。</p>
      </div>

      <div>
        <div class="font-medium mb-1 text-[var(--color-ink)]">流水线</div>
        <ol class="list-decimal pl-5 space-y-0.5">
          <li>yt-dlp 抓取音频，ffmpeg 转 16kHz 单声道</li>
          <li>SenseVoice / faster-whisper 自动转写，得到带时间戳的句子段</li>
          <li>pyannote/embedding 计算每段声纹，KMeans 聚类得到说话人 cluster</li>
          <li>LLM 修正同音错字（不改写文意），整理标题，按 utterance 翻译</li>
          <li>写入 .md（元数据）和 .yaml（对话）</li>
        </ol>
        <p class="mt-1">声音相似的组合（如同性别兄弟漫才）会有错配，需要在网页编辑器里手工调整。</p>
      </div>

      <div>
        <div class="font-medium mb-1 text-[var(--color-ink)]">本地编辑</div>
        <p>fork web 仓库，<code>npm install &amp;&amp; npm run dev</code>，详情页右上「编辑」按钮：</p>
        <ul class="list-disc pl-5 mt-1 space-y-0.5">
          <li>点说话人名字 → 切换</li>
          <li>点文字 → 直接编辑</li>
          <li>行末 ⋮ → 上下移动 / 插入 / 删除</li>
          <li>保存按钮直接写入本地 yaml 文件</li>
        </ul>
        <p class="mt-1">列表页有「+ 新增条目」入口，可手工录入新作品。这些入口仅在本地 dev 模式下出现，已部署的 GitHub Pages 是只读静态站。</p>
      </div>

      <div>
        <div class="font-medium mb-1 text-[var(--color-ink)]">贡献流程</div>
        <ol class="list-decimal pl-5 space-y-0.5">
          <li>fork <a class="underline" href="https://github.com/manzai-archive/web" target="_blank" rel="noopener">web 仓库</a></li>
          <li>本地 dev 中编辑，或在 GitHub 网页直接改对应 yaml（每个详情页右上有直链）</li>
          <li>commit 后提 PR</li>
        </ol>
      </div>
    </div>
  </details>

  <p class="text-[13px]" style="color: var(--color-ink-faint)">
    维护：<a class="underline" href="https://github.com/enkerewpo" target="_blank" rel="noopener">wheatfox</a>　·
    仓库：<a class="underline" href="https://github.com/manzai-archive/web" target="_blank" rel="noopener">web</a> /
    <a class="underline" href="https://github.com/manzai-archive/pipeline" target="_blank" rel="noopener">pipeline</a>
  </p>
</Base>
